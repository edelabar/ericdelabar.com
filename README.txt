Site MUST be run in Apache to get proper URL rewriting.  Make sure mod-rewrite is enabled.

Jekyll version 2.5.3, something breaks with plugins in 3.0...

sudo gem install jekyll -v 2.5.3
sudo easy_install Pygments
sudo gem install titlecase albino sass bourbon bitters neat